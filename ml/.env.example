# Same Neon Postgres connection string as backend/.env — the ML pipeline
# reads/writes the same database directly, it does not go through the
# Node backend.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"
